# Module: Graphics — Events — Tradeshow Design

> **Status: Stub** — This module has not yet been written. When generating output for this module, apply defaults from `design-system/DESIGN.md` and flag the gap.

## What this module covers

Physical and large-format print assets for tradeshow presence: booth backdrops, banners, signage, handout materials, and branded merchandise. Managed by the Product design team.

## Format specifications

_To be defined. Should cover: large-format bleed and safe zone requirements, CMYK color values (reference Pantone equivalents in PI-Colour-Palette.csv), minimum type sizes for large format, logo minimum sizes at large scale, booth layout hierarchy._

## Color note

Large-format print requires CMYK values. Use the `CMYK` column in `PI-Colour-Palette.csv` as the authoritative print color source. Pantone equivalents are listed in the `Pantone` columns for spot color matching.

## Segment overrides

Apply segment primary color per `segments/{segment}.md`.
